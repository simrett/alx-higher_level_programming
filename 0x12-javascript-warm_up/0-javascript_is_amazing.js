#!/usr/bin/node
// Ensure Node.js version compatibility (version 14.x)
// This code is semistandard compliant (version 16.x.x), following Standard + semicolons on top

const myVar = 'JavaScript is amazing';
console.log(myVar);

// Ensure there is a newline at the end of the file

